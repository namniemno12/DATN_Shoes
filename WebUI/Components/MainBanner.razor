@* Main Banner Component - Hero Style *@
<section class="main-banner">
    <div class="banner-container">
        <div class="banner-content-area">
            <div class="banner-badge">
                <span class="badge-pulse"></span>
                <span>🔥 Hot Deals</span>
            </div>
            <h1 class="banner-heading">
                Khám Phá Thế Giới
                <span class="gradient-text">Giày Thể Thao</span>
            </h1>
            <p class="banner-description">
                Bộ sưu tập mới nhất từ các thương hiệu hàng đầu. 
                Chất lượng cao, giá cả hợp lý, phong cách thời thượng.
            </p>
            <div class="banner-actions">
                <a href="#products" class="btn-shop-now">
                    Mua Sắm Ngay
                    <i class="fas fa-arrow-right"></i>
                </a>
                <div class="banner-stats">
                    <div class="stat">
                        <strong>1000+</strong>
                        <span>Sản phẩm</span>
                    </div>
                    <div class="stat">
                        <strong>50+</strong>
                        <span>Thương hiệu</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="banner-visual">
            <div class="visual-card card-sale">
                <div class="card-icon">💰</div>
                <div class="card-text">
                    <strong>-50%</strong>
                    <span>Flash Sale</span>
                </div>
            </div>
            <div class="visual-card card-shipping">
                <div class="card-icon">🚚</div>
                <div class="card-text">
                    <strong>Free Ship</strong>
                    <span>Orders 500K+</span>
                </div>
            </div>
            <div class="visual-card card-quality">
                <div class="card-icon">⭐</div>
                <div class="card-text">
                    <strong>100%</strong>
                    <span>Chính hãng</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .main-banner {
        background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        position: relative;
        overflow: hidden;
        padding: 80px 40px;
        margin-bottom: 60px;
    }

    .main-banner::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 70%);
        animation: float 15s ease-in-out infinite;
    }

    .banner-container {
        max-width: 1400px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
    }

    .banner-content-area {
        max-width: 600px;
    }

    .banner-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        padding: 8px 20px;
        border-radius: 50px;
        font-size: 14px;
        font-weight: 600;
        color: white;
        margin-bottom: 24px;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .badge-pulse {
        width: 8px;
        height: 8px;
        background: #ff6b6b;
        border-radius: 50%;
        animation: pulse 2s ease-in-out infinite;
    }

    .banner-heading {
        font-size: 56px;
        font-weight: 900;
        line-height: 1.2;
        color: white;
        margin-bottom: 20px;
        text-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }

    .gradient-text {
        display: block;
        background: linear-gradient(90deg, #fff 0%, #f0f0f0 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .banner-description {
        font-size: 18px;
        line-height: 1.6;
        color: rgba(255, 255, 255, 0.9);
        margin-bottom: 32px;
    }

    .banner-actions {
        display: flex;
        align-items: center;
        gap: 32px;
        flex-wrap: wrap;
    }

    .btn-shop-now {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        background: white;
        color: #1a1a1a;
        padding: 16px 32px;
        border-radius: 12px;
        font-size: 16px;
        font-weight: 700;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 8px 24px rgba(0,0,0,0.3);
    }

    .btn-shop-now:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(0,0,0,0.4);
        gap: 16px;
        background: #f5f5f5;
    }

    .banner-stats {
        display: flex;
        gap: 24px;
    }

    .stat {
        display: flex;
        flex-direction: column;
        color: white;
    }

    .stat strong {
        font-size: 28px;
        font-weight: 800;
        line-height: 1;
        margin-bottom: 4px;
    }

    .stat span {
        font-size: 13px;
        opacity: 0.9;
    }

    .banner-visual {
        position: absolute;
        top: 50%;
        right: 80px;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .visual-card {
        background: white;
        padding: 20px 24px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        gap: 16px;
        box-shadow: 0 12px 40px rgba(0,0,0,0.15);
        animation: floatCard 4s ease-in-out infinite;
        min-width: 200px;
    }

    .visual-card.card-sale {
        animation-delay: 0s;
    }

    .visual-card.card-shipping {
        animation-delay: 1s;
        margin-left: 40px;
    }

    .visual-card.card-quality {
        animation-delay: 2s;
    }

    .card-icon {
        font-size: 32px;
    }

    .card-text {
        display: flex;
        flex-direction: column;
    }

    .card-text strong {
        font-size: 18px;
        font-weight: 700;
        color: #1a1a1a;
        line-height: 1;
        margin-bottom: 4px;
    }

    .card-text span {
        font-size: 13px;
        color: #666;
    }

    @@keyframes float {
        0%, 100% { transform: translate(0, 0); }
        50% { transform: translate(-20px, -20px); }
    }

    @@keyframes floatCard {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
    }

    @@keyframes pulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.5; transform: scale(1.2); }
    }

    /* Responsive */
    @@media (max-width: 1024px) {
        .banner-visual {
            position: static;
            transform: none;
            margin-top: 40px;
        }

        .visual-card.card-shipping {
            margin-left: 0;
        }
    }

    @@media (max-width: 768px) {
        .main-banner {
            padding: 60px 24px;
        }

        .banner-heading {
            font-size: 36px;
        }

        .banner-description {
            font-size: 16px;
        }

        .banner-actions {
            flex-direction: column;
            align-items: flex-start;
        }

        .btn-shop-now {
            width: 100%;
            justify-content: center;
        }

        .banner-visual {
            gap: 12px;
        }

        .visual-card {
            padding: 16px 20px;
            min-width: auto;
        }

        .card-icon {
            font-size: 24px;
        }
    }
</style>
